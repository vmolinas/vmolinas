name: Update README cards

on:
  schedule:
    - cron: "0 0 * * *" # Corre una vez por día a medianoche UTC
  workflow_dispatch: # Te permite correrlo manualmente desde la pestaña Actions

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v6

      - name: Generate stats card
        uses: stats-organization/github-readme-stats-action@v2
        with:
          card: stats
          options: username=vmolinas&show_icons=true&theme=dark&hide_border=true&bg_color=161B22&count_private=true
          path: profile/stats.svg
          token: ${{ secrets.PAT }}

      - name: Generate top languages card
        uses: stats-organization/github-readme-stats-action@v2
        with:
          card: top-langs
          options: username=vmolinas&layout=compact&theme=dark&hide_border=true&bg_color=161B22&count_private=true
          path: profile/top-langs.svg
          token: ${{ secrets.PAT }}

      - name: Commit cards
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add profile/*.svg
          git commit -m "Update README cards" || exit 0
          git push
